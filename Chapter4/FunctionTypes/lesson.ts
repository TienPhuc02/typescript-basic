const sum = (x: number, y: number): number => {
  return x + y;
  //   return "abc";
};
console.log(">>check sum=", sum(1, 2));

let a = 10; //number
// a= sum(1,2)/string
//định nghĩa từng giá trị, định nghĩa giá trị trả về
