const sum = (a: number, b: number): number => {
  return a + b;
};
//funtionc làm chức năng lưu data, gọi API
const handleLogs = (message: string): void => {
  console.log(message);
};
//không trả ra gì quan tâm đến logic thoi
